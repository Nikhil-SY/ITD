That’s a huge ask — “everything” from the official docs would basically be a book. Instead, I’ll give you a **complete, interview-ready + real-world practical guide** that covers all core concepts, commands, and YAML fields you actually need as a DevOps engineer.

---

# 🚀 Kubernetes Deployment – Complete Guide

---

# 1. What is a Deployment?

## Definition

A **Deployment** in Kubernetes is a higher-level object used to:

* Manage **stateless applications**
* Provide **declarative updates**
* Ensure desired number of Pods are running

👉 It uses **ReplicaSet internally**

---

# 2. Deployment Architecture

```
Deployment
   ↓
ReplicaSet
   ↓
Pods
   ↓
Containers
```

---

# 3. Why Deployment?

## Without Deployment:

* You manually create Pods ❌
* No auto-healing ❌
* No scaling ❌

## With Deployment:

* Auto-healing ✔️
* Rolling updates ✔️
* Rollback ✔️
* Scaling ✔️

---

# 4. Basic Deployment YAML

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment
  labels:
    app: nginx
spec:
  replicas: 3
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: nginx
        image: nginx:1.25
        ports:
        - containerPort: 80
```

---

# 5. YAML Fields Explained (VERY IMPORTANT)

## 🔹 apiVersion

* Defines API version
* For Deployment → `apps/v1`

---

## 🔹 kind

* Type of resource

```yaml
kind: Deployment
```

---

## 🔹 metadata

```yaml
metadata:
  name: nginx-deployment
  labels:
    app: nginx
```

* `name` → unique resource name
* `labels` → key-value for grouping

---

## 🔹 spec (Main Section)

### 🔸 replicas

```yaml
replicas: 3
```

* Number of Pods

---

### 🔸 selector (CRITICAL)

```yaml
selector:
  matchLabels:
    app: nginx
```

* Must match Pod labels
* Used to identify Pods managed by Deployment

---

### 🔸 template (Pod Template)

```yaml
template:
```

This is the **actual Pod definition**

---

### 🔸 template.metadata.labels

```yaml
labels:
  app: nginx
```

* Must match selector

---

### 🔸 template.spec.containers

```yaml
containers:
- name: nginx
  image: nginx:1.25
```

---

### 🔸 container fields

#### name

* Container name

#### image

* Docker image

#### ports

```yaml
ports:
- containerPort: 80
```

---

# 6. Important Advanced Fields

---

## 🔹 resources (CPU/Memory)

```yaml
resources:
  requests:
    memory: "64Mi"
    cpu: "250m"
  limits:
    memory: "128Mi"
    cpu: "500m"
```

---

## 🔹 env (Environment Variables)

```yaml
env:
- name: ENV
  value: "prod"
```

---

## 🔹 envFrom (ConfigMap / Secret)

```yaml
envFrom:
- configMapRef:
    name: my-config
```

---

## 🔹 volume & volumeMounts

```yaml
volumeMounts:
- name: my-volume
  mountPath: /data

volumes:
- name: my-volume
  emptyDir: {}
```

---

## 🔹 livenessProbe

```yaml
livenessProbe:
  httpGet:
    path: /
    port: 80
  initialDelaySeconds: 10
  periodSeconds: 5
```

---

## 🔹 readinessProbe

```yaml
readinessProbe:
  httpGet:
    path: /
    port: 80
```

---

## 🔹 startupProbe (for slow apps)

---

# 7. Deployment Strategies

---

## 🔹 Rolling Update (Default)

```yaml
strategy:
  type: RollingUpdate
  rollingUpdate:
    maxUnavailable: 1
    maxSurge: 1
```

### Behavior:

* Gradual replacement of Pods

---

## 🔹 Recreate

```yaml
strategy:
  type: Recreate
```

### Behavior:

* Delete all old Pods → create new ones

---

# 8. Commands (VERY IMPORTANT)

---

## 🔹 Create Deployment

```bash
kubectl apply -f deployment.yaml
```

---

## 🔹 Get Deployments

```bash
kubectl get deployments
```

---

## 🔹 Describe

```bash
kubectl describe deployment nginx-deployment
```

---

## 🔹 Delete

```bash
kubectl delete deployment nginx-deployment
```

---

## 🔹 Scale

```bash
kubectl scale deployment nginx-deployment --replicas=5
```

---

## 🔹 Update Image

```bash
kubectl set image deployment/nginx-deployment nginx=nginx:1.26
```

---

## 🔹 Rollout Status

```bash
kubectl rollout status deployment nginx-deployment
```

---

## 🔹 Rollback

```bash
kubectl rollout undo deployment nginx-deployment
```

---

## 🔹 Rollout History

```bash
kubectl rollout history deployment nginx-deployment
```

---

# 9. Labels & Selectors (IMPORTANT INTERVIEW)

---

## Labels

```yaml
labels:
  app: nginx
  env: prod
```

---

## Selector Types

### matchLabels

```yaml
matchLabels:
  app: nginx
```

### matchExpressions

```yaml
matchExpressions:
- key: app
  operator: In
  values:
  - nginx
```

---

# 10. ReplicaSet Relation

Deployment automatically creates:

```bash
kubectl get rs
```

---

# 11. Update Flow (Internal Working)

1. New Deployment applied
2. New ReplicaSet created
3. Old ReplicaSet scaled down
4. New ReplicaSet scaled up

---

# 12. Zero Downtime Deployment

Achieved using:

* Rolling updates
* Readiness probe

---

# 13. Best Practices

✔ Always use labels properly
✔ Use readiness & liveness probes
✔ Set resource limits
✔ Use rolling updates
✔ Store config in ConfigMap/Secret
✔ Avoid `latest` image tag

---

# 14. Real-World Production YAML

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: my-app
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: my-app
    spec:
      containers:
      - name: app
        image: my-app:v1
        ports:
        - containerPort: 8080
        resources:
          requests:
            cpu: "200m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "256Mi"
        env:
        - name: ENV
          value: "prod"
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 15
```

---

# 15. Common Mistakes

❌ Selector and labels mismatch
❌ No resource limits
❌ Using latest image
❌ No probes
❌ Hardcoding configs

---

# 16. Interview Questions

### Q1: Difference between Deployment & ReplicaSet?

* Deployment manages ReplicaSet
* ReplicaSet manages Pods

---

### Q2: How rollback works?

* Uses previous ReplicaSet

---

### Q3: What ensures zero downtime?

* Rolling updates + readiness probe

---

### Q4: What happens if a Pod crashes?

* ReplicaSet recreates it

---

# 🔥 Final Summary

Deployment = **Automation layer for Pods**

* Manages lifecycle
* Enables updates
* Provides scalability
* Ensures reliability

---

# 👉 If you want next level:

I can explain:

* **Blue-Green vs Canary in Kubernetes**
* **Deployment with Service + Ingress**
* **Helm charts for Deployment**
* **Real interview scenario questions**

Just tell 👍
